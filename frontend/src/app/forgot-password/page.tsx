"use client";
import Link from "next/link";import {FormEvent,useState} from "react";import {api} from "@/lib/api";
export default function Forgot(){const[sent,setSent]=useState(false);const[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);try{await api("/auth/forgot-password/",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:fd.get("email")})});setSent(true)}catch(e){setError(e instanceof Error?e.message:"Please try again")}}return <main className="grid min-h-screen place-items-center bg-cream p-6"><div className="w-full max-w-md bg-white p-9 shadow-sm"><Link href="/login" className="text-xs text-stone">← Back to sign in</Link><p className="eyebrow mt-10">Account recovery</p><h1 className="mt-4 font-serif text-4xl">Reset your password.</h1>{sent?<div className="mt-8 border-l-2 border-gold bg-cream p-5 text-sm leading-6">If that email belongs to an active account, reset instructions have been sent.</div>:<><p className="mt-3 text-sm leading-6 text-stone">Enter the email associated with your customer account.</p><form onSubmit={submit} className="mt-8 space-y-5"><label><span>Email address</span><input name="email" type="email" required/></label>{error&&<p className="text-sm text-red-600">{error}</p>}<button className="btn-dark w-full">Send reset instructions</button></form></>}</div></main>}
