"use client";
import {FormEvent,useState} from "react";import {api} from "@/lib/api";
import {Captcha} from "@/components/captcha";
import {AnimatedHeading} from "@/components/animated-heading";
export function MeetingForm(){const[state,setState]=useState<"idle"|"busy"|"error"|"success">("idle");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setState("busy");const form=event.currentTarget,data=Object.fromEntries(new FormData(form));try{await api("/meeting-requests/",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});form.reset();setState("success")}catch{setState("error")}}return <><form onSubmit={submit} className="ref-meeting-form"><AnimatedHeading>Schedule a meeting</AnimatedHeading><input name="customer_name" placeholder="Full Name *" required/><input name="phone" placeholder="Phone Number *" required/><input name="email" type="email" placeholder="Email Address"/><select name="meeting_type" required defaultValue=""><option value="" disabled>Meeting type</option><option value="OFFICE">Office meeting</option><option value="SITE_VISIT">Site visit</option><option value="ONLINE">Online consultation</option></select><div className="grid grid-cols-2 gap-3"><input name="preferred_date" type="date" required/><input name="preferred_time" type="time" required/></div><textarea name="message" placeholder="Tell us what you are looking for" rows={3}/><Captcha/>{state==="error"&&<p className="meeting-error">Please complete the verification and check your details.</p>}<button disabled={state==="busy"}>{state==="busy"?"SENDING…":"SUBMIT REQUEST"}<span>→</span></button></form>{state==="success"&&<div className="meeting-success-backdrop" role="presentation" onClick={()=>setState("idle")}><div className="meeting-success-dialog" role="dialog" aria-modal="true" aria-labelledby="meeting-success-title" onClick={event=>event.stopPropagation()}><button className="meeting-success-close" type="button" onClick={()=>setState("idle")} aria-label="Close confirmation">×</button><span className="meeting-success-check">✓</span><p>Meeting request received</p><h2 id="meeting-success-title">Your next home starts with a conversation.</h2><div>Thank you for reaching out. One of our property advisors will contact you shortly to confirm your preferred meeting time.</div><button className="meeting-success-action" type="button" onClick={()=>setState("idle")}>Done</button></div></div>}</>}
