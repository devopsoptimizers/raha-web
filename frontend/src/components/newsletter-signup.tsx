"use client";
import {FormEvent,useState} from "react";import {API_URL} from "@/lib/api";
export function NewsletterSignup(){const[state,setState]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setState("Sending…");const form=new FormData(e.currentTarget);try{const r=await fetch(`${API_URL}/newsletter/subscribe/`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:form.get("email")})});if(!r.ok)throw new Error();setState("Thank you for subscribing.");e.currentTarget.reset()}catch{setState("Unable to subscribe right now. Please try again.")}}return <form className="newsletter-signup" onSubmit={submit}><input type="email" name="email" required placeholder="Your email address" aria-label="Email address"/><button>Subscribe</button>{state&&<p role="status">{state}</p>}</form>}
